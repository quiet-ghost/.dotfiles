---
description: Load spec-planner skill for dialogue-driven specification planning
---

Load the `spec-planner` skill and use it to help with the user request.

First, invoke the skill tool to load the spec-planner skill:

```
skill({ name: 'spec-planner' })
```

Then follow the skill instructions to complete the request.

<user-request>
$ARGUMENTS
</user-request>
