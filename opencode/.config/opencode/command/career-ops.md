---
description: Career Ops job search command center
---

Load the `career-ops` skill and use it to handle the request.

First, invoke the skill tool:

```text
skill({ name: 'career-ops' })
```

Then follow the skill instructions for this request:

<user-request>
$ARGUMENTS
</user-request>
