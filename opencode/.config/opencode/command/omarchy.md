---
description: Load Omarchy skill for Linux desktop and window manager configuration
---

Load the `Omarchy` skill and use it to help with the user request.

First, invoke the skill tool to load the Omarchy skill:

```
skill({ name: 'Omarchy' })
```

Then follow the skill instructions to complete the request.

<user-request>
$ARGUMENTS
</user-request>
