---
description: Load sst skill and help with SST infrastructure tasks
---

Load the `sst` skill and use it to help with the user request.

First, invoke the skill tool to load the sst skill:

```
skill({ name: 'sst' })
```

Then follow the skill instructions to complete the request.

<user-request>
$ARGUMENTS
</user-request>
