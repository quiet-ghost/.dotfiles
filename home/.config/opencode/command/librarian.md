---
description: Load librarian skill for multi-repository code exploration
---

Load the `librarian` skill and use it to help with the user request.

First, invoke the skill tool to load the librarian skill:

```
skill({ name: 'librarian' })
```

Then follow the skill instructions to complete the request.

<user-request>
$ARGUMENTS
</user-request>
