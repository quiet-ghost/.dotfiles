{
  "name": "ghost-pi-config",
  "private": true,
  "type": "module",
  "workspaces": [
    "agent/extensions/*"
  ],
  "scripts": {
    "check": "tsc --noEmit && npm run check --workspace=pi-extension-opencode-cloudflare && npm run check --workspace=pi-codex-fast-variants-extension && npm run check --workspace=pi-save-md-extension && npm run check --workspace=pi-cloak-extension && npm run typecheck --workspace=pi-skill-toggle && npm run test --workspace=pi-skill-toggle",
    "test:codex-fast-mode": "node --import tsx --test agent/extensions/codex-fast-variants/*.test.ts",
    "test:save-md": "npm run test --workspace=pi-save-md-extension",
    "test:worker-configuration-guard": "node --import tsx --test agent/extensions/tests/worker-configuration-guard.test.ts"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.84.0",
    "@earendil-works/pi-coding-agent": "^0.84.0",
    "@earendil-works/pi-tui": "^0.84.0",
    "typebox": "^1.1.38"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
