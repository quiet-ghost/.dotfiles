#!/bin/bash
set -euo pipefail

pi_bin="$HOME/.local/share/bun/bin/pi"

if [[ ! -x $pi_bin ]]; then
  echo "pi is not installed. Run: bun add --global @earendil-works/pi-coding-agent@latest" >&2
  exit 127
fi

PLANNOTATOR_PORT=19433 exec "$pi_bin" "$@"
